# bag_plot

This project helps visualizing the data in a downloaded AutoXing bag file.

It's function now is very limited. Only added according to the needs of customers.

It requires ROS1 environment.

```
./plot.py sample.bag --phase-current
```
